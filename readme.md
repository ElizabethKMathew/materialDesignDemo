# Webpack starter kit

* Clone the project. Checkout to branch 'Webpack-starter-kit'

* npm install

* to run application in development mode: npm start

* to run application in production mode: npm run build
